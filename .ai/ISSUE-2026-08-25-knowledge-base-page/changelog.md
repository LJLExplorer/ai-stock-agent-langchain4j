# 变更历史

## 2026-08-25

- 根据用户需求创建知识库管理页面 Issue。
- 确认采用 `react-router-dom` 标准路由方案。
- 确认范围：真实新增、查看、禁用、删除；禁用时同步清理 RAG 向量；保留问答返回入口。

## 实现记录

- 已接入 React Router，新增 `/knowledge` 知识库页面和问答/知识库导航。
- 已完成文档新增、列表查看、禁用、删除及前端反馈交互。
- 后端禁用会先清理向量，再保存禁用状态，避免已禁用文档继续参与 RAG。
- 前端构建和后端生产编译通过；全量 Maven 测试被工作区已有的缺失 `org.bsc.langgraph4j` 依赖阻塞。
