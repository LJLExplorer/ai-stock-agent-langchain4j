# 变更历史

## 2026-08-24

- 创建会话记忆与 RAG 诊断 Issue。
- 明确以 `HEAD(main)` 为实现基线。
- 将短期记忆方案确定为 Redis List + 字符数滑动窗口 + 滚动摘要。
- 将长期记忆方案确定为主动录入 + Embedding + Milvus + MongoDB 元数据。
- 增加轻量 RAG 诊断需求，用于区分召回问题、模型问题和链路异常。
- 明确不保存完整模型输入、输出和增强上下文。

## 实现更新

- 完成 Redis List 短期记忆、32K 字符窗口和滚动摘要。
- 完成长期记忆主动录入、Milvus 向量化、MongoDB 元数据和用户隔离召回。
- 增加长期记忆 REST API 和前端主动录入入口。
- 将长期记忆和短期摘要接入 ChatService 当前问题上下文。
- 增加轻量 RAG 诊断记录，记录召回数量、最高相似度、来源和回答长度。
